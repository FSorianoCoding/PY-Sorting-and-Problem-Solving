# Write your solution for algorithm 2 below
# Write an algorithm that takes in an unsorted numerical list as an argument 
# and creates a new sorted list (use the sorted() function).

a = [6, 3, 4, 1, 8]
a_sorted = sorted(a)

print(a_sorted)